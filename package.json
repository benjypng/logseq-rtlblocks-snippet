{
  "logseq": {
    "id": "logseq-rtlblocks-plugin",
    "title": "logseq-rtlblocks-plugin",
    "icon": "./icon.svg"
  },
  "license": "MIT",
  "name": "logseq-rtlblocks-plugin",
  "main": "dist/index.html",
  "scripts": {
    "build": "rm -R dist && echo 'dist folder removed'; parcel build --no-source-maps src/index.html --public-url ./"
  },
  "targets": {
    "main": false
  },
  "dependencies": {
    "@logseq/libs": "^0.0.14"
  },
  "devDependencies": {
    "parcel": "^2.8.2"
  }
}
